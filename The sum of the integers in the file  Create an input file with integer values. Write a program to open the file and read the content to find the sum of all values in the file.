#include<iostream>
#include<fstream>
using namespace std;
int main(){
    int n,value,sum=0;
    if(!(cin>>n)) return 0;
    ofstream file;
    file.open("sample.txt");
    for(int i=0;i<n;i++){
        cin>>value;
        file<<value<<" ";
    }
    file.close();
    ifstream readfile("sample.txt");
    while(readfile >> value){
        sum+=value;
    }
    readfile.close();
    cout<<sum;
    return 0;
}
